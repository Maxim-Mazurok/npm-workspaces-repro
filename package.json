{
  "name": "npm-test",
  "scripts": {
    "test": "npm test --workspaces",
    "workaround": "npm run workaround --workspaces"
  },
  "workspaces": [
    "jest-26",
    "jest-28"
  ],
  "engines": {
    "node": "18.3.0",
    "npm": "8.12.1"
  }
}
